/**
 * Parking photos. Public (visible in search results), stored in S3-compatible
 * object storage behind an interface; the upload endpoint returns 501 with a
 * clear message when storage is not configured. (Phase 2: entity; Phase 4: endpoints.)
 */
package com.spotshare.photo;
