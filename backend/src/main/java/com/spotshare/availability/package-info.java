/**
 * Temporary host availability — the "I'm leaving / Share My Spot" flow.
 * Windows, the derived display states, the share/remove service, and the
 * host availability endpoints. Phase 3 creates MANUAL windows only; COMMUTE
 * and VACATION materializers (Phases 8–9) reuse the same table.
 */
package com.spotshare.availability;
