/**
 * Parking space listings. Listing requires the host's authorization
 * confirmation (authorization_confirmed + authorization_confirmed_at).
 * (Phase 2: ParkingSpace entity + repository; Phase 4: spaces CRUD.)
 */
package com.spotshare.space;
