/**
 * User accounts: the {@code User} entity, {@code Role}, and refresh-token
 * persistence backing the rotating refresh tokens in {@code com.spotshare.auth}.
 * Host and driver are personas, not roles — any user can list spaces and reserve.
 */
package com.spotshare.user;
